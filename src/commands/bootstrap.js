export const command = 'bootstrap';
export const builder = {};
export const desc = 'clone, link and install all named repos';
export function handler() {
  console.log('bootstrapping ganon'); // eslint-disable-line no-console
}
